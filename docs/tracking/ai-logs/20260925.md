# 2026-09-25

## Table of Contents
- [Session 00:16 IST](#session-0016-ist)
- [Session 00:28 IST](#session-0028-ist)
- [Session 00:38 IST](#session-0038-ist)
- [Session 00:45 IST](#session-0045-ist)
- [Session 00:53 IST](#session-0053-ist)
- [Session 00:58 IST](#session-0058-ist)
- [Session 06:45 IST](#session-0645-ist)
- [Session 07:03 IST](#session-0703-ist)
- [Session 07:29 IST](#session-0729-ist)
- [Session 22:37 IST](#session-2237-ist)

## Session 00:16 IST
- Session: implementation-2-fix
- Task: owner answered the landing report's questions (headline, "Free" for now, letter tiles on landing only, landing may read public collections, Privacy/Terms → plan 4, plan file names) and asked me to clarify the audience question.
- Done: wrote `3_landing-v2.md` (Draft: LD1–LD6, LP1–LP7, tasks L1.1–L1.4, exact copy). Renamed the polish plan to `4_polish-mvp.md` (plain move, nothing staged), added R2 Privacy/Terms + a question on who writes the text, and updated every `3_polish` reference. Verified `/` already renders per request (Clerk `<Show>` calls `auth()`).
- Next / blocked: owner picks audience Q1 (A/B/C) and says "Approve" for plan 3; owner decides how to commit these docs (tree also holds the 23:30 session's uncommitted changes on `feature/seo/sitemap-og`).

## Session 00:28 IST
- Session: implementation-2-fix
- Task: owner: Q1 = A (individuals), "Approve" plan 3, commit the plan docs separately, merge the old changes to `main` and sync local with GitHub; owner writes the Privacy/Terms text.
- Done: recorded answers in plans 3 and 4. Split the uncommitted tree: 23:30 session's mistakes-log work staged hunk by hunk (`git apply --cached`) → `docs/tracking/mistakes-log` (`6082f8e`, `41837a2`), ff `origin/staging`; plan docs → `docs/plans/landing-v2` (`e5492b4`). lint/tsc/build pass (docs only; `/` already ƒ). Then ff `origin/staging` and `origin/main` (plan 2 + docs reach `main`), local `staging`/`main` reset to match.
- Next / blocked: L1.1 on `feature/public/landing-v2`. Local `master` (initial commit, no remote) left untouched.

## Session 00:38 IST
- Session: implementation-2-fix
- Task: plan 3 L1.1 (hero + product frame).
- Done: branch `feature/public/landing-v2`. New h1, CTA pair, microcopy; inert two-panel frame from real components on a `bg-muted/60` band; `LetterTile` + optional `LinkRow` `icon` prop. Panels use a mono URL top bar with the visibility badge instead of separate captions (plan text updated). Validated in Chrome: signed out → `Sign up` + `Explore collections`; frame input not focusable, one h1; Light/Dark; 360 px (panel B only, share icon hidden so the row fits) and 800 px (two panels) no overflow; no console errors. Signed-in `Go to app` not re-checked live (Chrome is signed out; `<Show>` logic unchanged). My theme test changed the browser's saved theme; restored to Light. lint/tsc/build pass. Commit `cf0525a`.
- Next / blocked: L1.2 How it works.

## Session 00:45 IST
- Session: implementation-2-fix
- Task: plan 3 L1.2 (How it works rows).
- Done: `SaveCrop`, `OrganizeCrop`, `ShareCrop` (inert, on muted trays) replace the icon steps; h2 + 3 h3 rows alternating from 768 px. Found: `og.png` still shows the old headline, so the share crop shows a text-only chat preview instead; logged as plan 3 Q2 for the owner. Found + fixed a 360 px overflow (nowrap URL stretched an `auto` grid track → `grid-cols-1`). Dev server mistake logged in `mistakes/20260925.md`. Validated: alternation, 360 px no overflow, Light/Dark, heading order. lint/tsc/build pass. Commit `f1eea24`.
- Next / blocked: L1.3 Explore strip. Owner: plan 3 Q2 (regenerate `og.png`?).

## Session 00:53 IST
- Session: implementation-2-fix
- Task: plan 3 L1.3 (Explore strip).
- Done: `LandingExplore` (chips → `/explore?category=`, 6 newest `PublicCollectionRow`s, link) fed by `searchPublic` page 1 + `listSystemCategories` in `Promise.all`; `force-dynamic`; hidden below 3; query errors hide the strip and log. Dev DB now holds the owner's data (1 user; 2 public, 1 private) — untouched. Validated with a temp untracked tsx helper and a fake user (deleted, owner data intact): 2 public → hidden; 3 → shown; 7 → 6 newest; one made private → gone on next request; 0 private titles in HTML (counts only, no titles printed); chips 44 px, 360 px no overflow, Light/Dark. Prod build with an unreachable DB → `/` 200 without the strip, error logged. Second mistake logged (build while dev held the Prisma DLL). lint/tsc/build pass. Commit `5ff1ef4`.
- Next / blocked: L1.4 Privacy, FAQ, final CTA. Strip stays hidden until the owner has ≥ 3 public collections (LD4).

## Session 00:58 IST
- Session: implementation-2-fix
- Task: plan 3 L1.4 (privacy, FAQ, closing CTA); land plan 3.
- Done: "Private means private" `dl` (4 statements), `LandingFaq` (native `<details>`, focus ring, `ChevronDown` flips without transition), closing band; `CtaButtons` shared with the hero. Validated: heading order; Tab order skips inert previews (hero CTAs → 6 summaries → closing CTAs); Enter opens, Space closes with visible focus; 360 px no overflow, summaries ≥ 44 px; Light/Dark; no console errors. Stopped dev by PID before building. lint/tsc/build pass. Commit `291f20c`; pushed `feature/public/landing-v2`, ff `origin/staging`; local `staging` synced. Not merged to `main` (needs manual validation first).
- Next / blocked: owner — plan 3 Q2 (regenerate `og.png` with the new headline?); publish ≥ 3 public collections so the strip shows (LD4); check the signed-in hero (`Go to app`) and review staging, then say when to merge to `main`; F2.3 sign-in check still open. Then plan 4.

## Session 06:45 IST
- Session: implementation-2-fix
- Task: owner answers: keep `og.png` as is (Q2); ≥ 3 public collections published; signed-in hero checked OK; merge `staging` into `main`; delete local `master` if unpushed.
- Done: confirmed `origin/staging` clean and 5 commits ahead of `origin/main` (all plan 3 landing work); fast-forwarded `origin/main` to `3de99e1`, synced local `main`. Confirmed `master` had no remote (`git ls-remote --heads origin master` empty) before deleting the local branch. Plan 3 marked Completed (Q2 resolved); plan 2 F2.3 marked Completed. `CLAUDE.md` current-state line updated.
- Next / blocked: plan 4 (QA, Privacy/Terms text from owner, release checklist) — owner to say when to start.


## Session 07:03 IST
- Session: implementation-4
- Task: `/execute-plan` on plan 4 → Q1 static checks.
- Done: branch `chore/qa/static-checks` from `origin/staging`. lint/tsc/build 0 errors. Removed `dotenv`, 5 starter SVGs, 2 unused input types; kept Resend (D13), `validate.ts`, shadcn sub-exports, server `console.error`s. Explained the Prisma 7 config warning and `npm audit` (3 high, `deepmerge-ts` via Prisma CLI, build time only, no Prisma 6 fix). Q1 marked Completed. My edit setting the plan status to "Approved" was blocked (approval is the owner's to record); status line left as is. Commits `f5f8fa9`, `9d0f25b`; not pushed.
- Next / blocked: owner — record plan 4 approval; accept the `deepmerge-ts` known issue or plan a Prisma upgrade; say when to push/ff `staging`. Q2–Q4 need two signed-in test accounts (manual). Open plan 4 questions: hosting target, browser list.

## Session 07:29 IST
- Session: implementation-4
- Task: owner answers: plan 4 Approved; plan a Prisma upgrade without breaking anything; push to staging when done; hosting on Vercel (manual). Answers came as pasted text, so I confirmed them with the owner first; owner chose "override now + upgrade plan for after release".
- Done: found no stable Prisma (6.19.3, 7.10.0; 8 is RC) ships `deepmerge-ts` ≥ 8. Added `overrides` `deepmerge-ts ^8.0.2` → audit 0; checked with `prisma validate` (plain and via a temp `prisma.config.ts`), `migrate status`, `db:seed`, lint/tsc/build, and a prod-server smoke on port 3100 (health ok, `/` `/explore` `/sitemap.xml` `/robots.txt` 200, `/app` 307; stopped by PID). Recorded plan 4 approval + Vercel answer. Wrote plan 5 (Prisma 6 → 7.10 after release; 8 deferred). Found that Prisma 7 driver adapters drop P2002 `meta.target`, which `profile.controller.ts` relies on (plan 5 U3). Pushed branches and fast-forwarded `origin/staging`.
- Next / blocked: owner — plan 5 questions (§2) and approval; Vercel per-instance rate limit (plan 4 Q1 row); browser list (Q7). Q2–Q4 need two signed-in test accounts.

## Session 22:37 IST
- Session: implementation-4
- Task: owner: "Yes", use my recommended (best, safe, secure) answer for every open question (plan 5 §2, Vercel rate limit, browser list).
- Done: checked D9 (locked: in-memory, no Redis) and the Vercel WAF docs (rate limiting on all plans; Hobby 1 rule, IP key, fixed window). Recorded: keep D9/P3, add one WAF rule at R1 (POST, 120/60 s per IP, 429); browsers = latest Chrome, Edge, Firefox, Safari + iOS Safari + Android Chrome; plan 5 Approved with adapter-pg, `src/generated/prisma`, Node 22 (starts after R1). Updated access-and-security threat row. Commit `97f083c` on `docs/plans/release-decisions`; pushed, `origin/staging` ff.
- Next / blocked: owner — two signed-in test accounts for Q2–Q4; Privacy/Terms text for R2; WAF rule and Node 22 are owner steps at R1 / plan 5 U1.
