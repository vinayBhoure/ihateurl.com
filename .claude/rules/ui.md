# UI

To be filled after `docs/implementation-plan/2_frontend-mvp.md` is approved (design tokens, spacing, component usage).

Until then:
- shadcn primitives from `src/components/ui`.
- lucide-react for icons.
- sonner for toasts.
