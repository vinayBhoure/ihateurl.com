# Architecture

Follow `docs/architecture/overview.md` for system context, layers, request flows and code layout.
Follow `docs/architecture/data-model.md` for schema, invariants I1–I9, delete behaviour and indexes before touching schema, queries or controllers.
